### Example environment variables for DBTeamV2
# Copy this file to `.env.local` and fill values. NEVER commit `.env.local` to git.

# App
WEB_API_KEY=changeme_api_key_here
WEB_API_SECRET=changeme_secret_here
WEB_API_PORT=5500

# Redis
REDIS_URL=redis://localhost:6379/0

# TDLib / client
TDLIB_PATH=/path/to/tdlib
TDLIB_DATA_DIR=/path/to/tdlib/data

# Database used by Nginx Proxy Manager (if running locally)
MYSQL_ROOT_PASSWORD=replace_me
MYSQL_DATABASE=npm
MYSQL_USER=npm
MYSQL_PASSWORD=replace_me

# Optional: Bot token (store only if you understand the risk)
#BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Other secrets
#JWT_SECRET=replace_with_long_random_value
#FERNET_KEY=replace_with_32_byte_base64

# Docker / deployment specific
DOCKER_ENV=development
